# Phiên bản đã kiểm chứng chạy được trên RK3588 / Ubuntu 22.04 (aarch64).
# Cài: pip install -r requirements.txt

fastapi==0.139.2
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
asyncpg==0.31.0
alembic==1.18.5
pydantic-settings==2.14.2
httpx==0.28.1
python-multipart==0.0.32

# ─── AI / thị giác máy tính ───────────────────────────────────────────
supervision==0.29.1
trackers==2.5.0.post0
opencv-python==5.0.0.93
numpy==2.2.6

# ─── Milvus: BẮT BUỘC ghim đúng, không được tự nâng/hạ ────────────────
# Định dạng dữ liệu trên đĩa GẮN CHẶT với phiên bản milvus-lite:
#   milvus-lite 2.x -> lưu 1 file SQLite
#   milvus-lite 3.x -> lưu 1 THƯ MỤC (collections/, databases/, LOCK)
# Lệch phiên bản giữa máy dev và máy deploy làm thư mục milvus_face.db
# không mở được, báo lỗi khó hiểu "unable to open database file" và toàn
# bộ khuôn mặt đã đăng ký thành không đọc được (đã xảy ra 21/07/2026).
# Đổi phiên bản 2 gói này thì phải backup milvus_face.db trước.
pymilvus[milvus-lite]==3.0.0
milvus-lite==3.1.0

# ─── Tiện ích / phần cứng ─────────────────────────────────────────────
python-Levenshtein==0.27.3
psutil==7.2.2
OPi.GPIO==0.5.2
python-vlc==3.0.21203
